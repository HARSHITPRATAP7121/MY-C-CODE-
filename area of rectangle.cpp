#include<stdio.h>
int main () {
  	float l ,b ,a =l*b ,p =2*l + 2*b ;
	scanf (" %f,%f", &l,&b) ;
	 // a =area of rectangle
	 // p= perimeter of rectangle
	printf (" area of rectangle = %f \n ", a);
 	printf (" area of rectangle = %f \n ", p);
 	
	return 0;
}
